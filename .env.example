# =============================================================================
# OpenClaw Marketplace — Environment Variables
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to git
# =============================================================================

# --- Database (Neon or Vercel Postgres) ---
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# --- NextAuth.js ---
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"

# --- GitHub OAuth (Settings > Developer settings > OAuth Apps) ---
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# --- Stripe ---
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# --- GitHub (for granting repo access after purchase) ---
# Personal Access Token with `repo` scope from the repo owner account
GITHUB_PAT="ghp_..."

# --- Resend (Email) ---
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@openclaw.design"

# --- Vercel Blob (File Storage) ---
BLOB_READ_WRITE_TOKEN=""

# --- Site ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
